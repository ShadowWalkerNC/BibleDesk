**PARKED — desktop Electron shell (out of scope for MVP).** The Electron desktop app in this directory was moved to `archive/desktop/` and is explicitly unsupported for the BibleDesk MVP: no desktop installer is built, distributed, or maintained, and the web app is the desktop story. Reviving it later would require re-verifying the prerequisites listed in the distribution deep-dive doc (research/deep/18-distribution-deep-dive.md §6) — a working Electron build/packaging pipeline on each target OS, code-signing certificates and notarization, an auto-update strategy, and product sign-off that a desktop shell adds value over the web app — then moving this directory back to `apps/desktop/` and updating the parked packaging scripts.
