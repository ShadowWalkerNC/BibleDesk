**PARKED — Chrome extension (out of scope for MVP).** The Manifest V3 side-panel extension in this directory was moved to `archive/extension/` and is explicitly unsupported for the BibleDesk MVP: no extension zip is produced and nothing is submitted to the Chrome Web Store. Reviving it later would require re-verifying the prerequisites listed in the distribution deep-dive doc (research/deep/18-distribution-deep-dive.md §6) — a real packaging step that produces a verified zip, a Chrome Web Store publisher account and review plan, keeping the manifest in lockstep with the web app's API contract, and product sign-off that the side panel adds value over the web app — then moving this directory back to `apps/extension/` and updating the parked packaging scripts.
